import React from "react";
import { View, Text } from "react-native";

export default function ApprovalScreen() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Visitor Approval Screen</Text>
    </View>
  );
}